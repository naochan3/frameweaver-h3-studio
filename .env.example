# P2 is not implemented. 1 makes frameweaverd fail closed at launch.
DISCORD_AUTH_ENABLED=0
# Optional JSON array matching config/workers.example.json. When omitted, only the local 4090 is used.
FRAMEWEAVER_WORKERS_JSON=
